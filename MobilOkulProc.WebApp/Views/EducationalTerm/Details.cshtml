@using MobilOkulProc.Entities.Concrete
@using MobilOkulProc.Entities.General
@model Mesajlar<EDUCATIONAL_TERM>

@{
    ViewData["Title"] = "Educational Term Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nesne.EducationTerm)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nesne.EducationTerm)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nesne.Status)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nesne.Status)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Düzenle", "Edit", new { id = Model.Nesne.ObjectID }) |
    @Html.ActionLink("Listeye Geri Dön", "List")
</div>
<p>
    <div class="input-group">
        @if (Model != null)
        {
            if (Model.Mesaj != null)
            {
                <div class="alert alert-@Model.Status" role="alert">@Model.Mesaj</div>
            }


        }
    </div>
    <br />

</p>